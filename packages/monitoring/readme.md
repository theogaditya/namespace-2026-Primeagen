## Install dependencies using legacy peer deps

```bash
npm install pg --legacy-peer-deps && npm install @types/pg --save-dev --legacy-peer-deps
```

### Set up .env in root of monitoring, and recreate it with refernce to the .env.example


